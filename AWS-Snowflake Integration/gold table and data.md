# SCHEMA

```sql
USE ROLE ACCOUNTADMIN;
USE DATABASE WALMART_DB;
CREATE SCHEMA GOLD;
USE SCHEMA GOLD;
```

# After creating GOLD layer tables verifying data

```sql
SELECT COUNT(*) FROM WALMART_DB.GOLD.WALMART_DATE_DIM; 
SELECT COUNT(*) FROM WALMART_DB.GOLD.WALMART_FACT_TABLE; 
SELECT COUNT(*) FROM WALMART_DB.GOLD.WALMART_STORE_DIM;
```
